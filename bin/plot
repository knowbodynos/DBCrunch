#!/bin/bash

ProgDir=$(cd $(dirname "${BASH_SOURCE[0]}"); pwd -P)

modpath=$1
controllerpath=$2
workpath=$3

python ${ProgDir}/tools/plot.py ${workpath} ${controllerpath} plot_TEMP.pdf plot_OUT.pdf 2>/dev/null